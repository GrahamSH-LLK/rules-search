export default defineAppConfig({
   ui: {
      tooltip: {
        slots: {
         content: "h-auto",
          text: 'max-w-64 overflow-visible text-wrap'
        }
      },
    
      colors: {
      primary: "blue",
      neutral: "zinc",
    },
  },
});
